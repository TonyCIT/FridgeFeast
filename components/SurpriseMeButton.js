// src/components/SurpriseMeButton.js

import React from 'react';

const SurpriseMeButton = ({ onClick }) => {
  return (
    <button onClick={onClick}>Surprise Me!</button>
  );
};

export default SurpriseMeButton;
